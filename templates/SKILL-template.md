---
name: <skill-name-kebab-case>
description: <プロダクト名>で<何をするとき>に使う。「<トリガーになる依頼フレーズ1>」「<フレーズ2>」「<フレーズ3>」を依頼されたら従う。<絶対に守らせたい原則を1文で>。
---

<!--
  ============================================================
  テンプレートの使い方
  ・description が最重要。Claudeがスキルを使うかどうかは description だけで判断される
    - 「何をするスキルか」+「どんな依頼フレーズで発動するか」を両方書く
    - Claudeはスキルを使い忘れがちなので、トリガー条件は少し過剰なくらい具体的に書く（pushy に）
  ・本文は500行以内。長くなるなら references/ に分割して「いつ読むか」を本文に書く
  ・手順は「着手前 → 実装 → テスト → 報告」の4段構成を推奨
    （phase-implementation スキルと同じ骨格。レビューしやすさのため）
  ・埋め終わったらこのコメントを削除する
  ============================================================
-->

# <スキル名> スキル

## 目的

<このスキルが保証したいこと。「何を作るか」ではなく「どういう状態で完了とするか」を1〜2行で>

## 原則

<!-- プロジェクト共通の原則は CLAUDE.md にあるので繰り返さない。
     このスキル固有の原則だけを書く -->
- <このタスク種別に固有の原則1>
- <原則2>
- 不明点は推測で埋めず、着手前に質問する

## 手順

### 1. 着手前（実装を書き始める前に必ず）

- 今回の「完了条件」を1〜2行で言語化し、依頼者に確認する
- 触るファイル・追加する関数・追加するテストを列挙する（触らない範囲も明示する）
- 既存仕様と衝突しうる点があれば洗い出し、判断を仰ぐ
- <このスキル固有の事前確認事項があれば追記>

### 2. 実装

- CLAUDE.md の「実装の型」に必ず合わせる
- <このスキル固有の実装手順・パターンを列挙>
  <!-- 例: コマンド追加なら「is_/extract_/handle_ の3点セットを作る」
       「prefix衝突を確認する」「mainディスパッチの順序を守る」のような
       機械的にチェックできる粒度で書くと事故が減る -->
- <手順2>
- <手順3>

### 3. テスト

- 正常系 + 境界値 + 異常系 のテストを付ける
- 既存テスト全件 green を維持する
- <このスキル固有のテスト観点があれば追記>
  <!-- 例: 「コマンド追加時は既存コマンドとのprefix衝突テストを必ず入れる」 -->

### 4. 報告（完了時に必ず含める）

- 変更ファイル一覧（未変更の主要ファイルも明示する）
- 追加・更新したテストとテスト結果（全件数・成否）
- 想定される不具合・境界値・残っているリスク
- <このスキル固有の報告事項があれば追記>

## やらないこと

- 完了条件に含まれない機能の先取り実装
- 「ついでに」の周辺リファクタ（別途提案する）
- <このスキル固有の禁止事項>
