# CLAUDE.md — <プロダクト名>

<!--
  ============================================================
  テンプレートの使い方
  ・【固有】マークの節はプロジェクト開始時に必ず埋める
  ・【共通】マークの節はそのまま使い回せる（開発思想なのでプロダクトに依存しない）
  ・【育成】マークの節は最初は空でよい。Phase 1〜2 を実装しながら追記していく
  ・埋め終わったら、このコメントと <> プレースホルダーをすべて削除する
  ============================================================
-->

## プロジェクト概要【固有】

- プロダクト名: <プロダクト名>
- 一言でいうと: <何をするものか1〜2行>
- 現在のフェーズ: <MVP開発中 / Phase N まで完了 など>
- 対象ユーザー: <誰が使うか>

## 技術スタック【固有】

- 言語: <Python 3.x / TypeScript など>
- 主要ライブラリ/フレームワーク: <FastAPI, React など。バージョン固定があれば明記>
- テスト: <pytest / vitest など>
- 実行環境: <ローカル / Docker / どのOSか>

## ディレクトリ構成【固有】

<!-- 主要ディレクトリだけでよい。全部書くと保守されなくなる -->
```
<project>/
├── src/          # <役割>
├── tests/        # <役割>
├── config/       # <役割>
└── reports/      # 生成物（コミット対象外）
```

## 開発コマンド【固有】

<!-- Claudeに毎回聞かれないための節。動作確認済みのものだけ書く -->
- 起動: `<コマンド>`
- テスト全実行: `<コマンド>`
- lint/format: `<コマンド>`

## 開発方針【共通】

- MVP優先。Phase単位（レビュー可能な最小の変更単位）で実装する
- 一気に全機能を実装しない。将来のPhaseの分を先取りしない
- 変更範囲を最小化する。依頼されていないファイル・関数に触らない
- 既存仕様を勝手に変更しない。挙動が変わる修正は実装前に必ず確認を取る
- 不明点は推測せず、着手前に質問する

## コード方針【共通】

- 初学者でも理解しやすいコードを書く。可読性 > 短さ・巧妙さ
- 複雑な最適化・過剰な抽象化を避ける
- 1関数=1責務。関数やクラスの責務を明確にする
- 「なぜその実装にしたか」をコメントで残す（「何をしているか」はコードで読めるので書かない）
- config化できる値（上限値・タイムアウト・パス・閾値）は定数化を提案する

## 実装の型【育成】

<!--
  このプロジェクト固有の「決まりごと」を、実装しながらここに集約する。
  例（AIつむぎでの実例。新プロジェクトでは合うものだけ残す）:
  - 例外は落とさず None/False/空 で返す
  - パス検証は realpath + commonpath + ValueError
  - JSON保存は atomic_write_json を使う
  - subprocess はリスト渡し（shell=True 禁止）
  - プロンプトの format 対象はテンプレのみ。外部入力はデリミタで封じ込める
-->
- <このプロジェクトの型を1つずつ追記>

## テスト方針【共通】

- 追加・変更した関数には 正常系 + 境界値 + 異常系 のテストを付ける
- 修正前に既存テスト全件 green を確認し、修正後も全件 green を維持する
- テストを消したり skip にして green にしない
- 完了報告には「想定される不具合・境界値・残っているリスク」を必ず含める

## Git運用【共通】

- 大きな変更前はコミットを提案する（確認なしで進めない）
- build成果物・生成物（reports/ 等）は勝手にコミットしない
- ファイル構成を変更する場合は、変更前に理由を説明する

## やらないこと【共通】

- 依頼されていない「ついでに」のリファクタ・整形（別Phaseとして提案する）
- 既存の挙動を黙って変えること
- 未確認のコマンドやライブラリを「動くはず」で導入すること

## 既知の制約・注意点【育成】

<!-- ハマりどころが見つかるたびにここへ。例: 「XXライブラリはv2系だと壊れる」 -->
- <随時追記>
